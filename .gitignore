# Dependencies
node_modules
.pnp
.pnp.js

# Local Claude Code tooling config (machine-specific paths; never commit)
.claude/

# Testing
coverage

# Production
build
dist
.next
out

# Misc
.DS_Store
*.pem
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# IDEs
.vs
.vscode
*.swp
*.swo
*~

# OS files
Thumbs.db
.DS_Store

# Build artifacts
.turbo
.cache
dist
build

# Environment
.env.local
.env.*.local

# IDE settings
.vscode/settings.json
.vscode/extensions.json
.idea/*
!.idea/runConfigurations

# Vercel
.vercel

# Next.js
.next/
out/

# Testing
.coverage
coverage/

# Temp
tmp/
temp/
*.tmp

# ── GitHub push-safety prep (additive) ────────────────────────────────
# NOTE: .env.example is intentionally NOT ignored (placeholders only, safe to
# track). Real secrets live in .env.local (ignored above).

# TypeScript incremental build cache
tsconfig.tsbuildinfo
*.tsbuildinfo

# Test results / screenshots / E2E reports (all generated, never source)
test-results/
playwright-report/
blob-report/
screenshots/
*.spec-results/
.last-run.json
*.webm
*.trace.zip

# Additional secret / certificate types (.env.* and *.pem already covered above)
*.key
*.crt
*.p12
*.pfx

# Logs (general)
*.log
pnpm-debug.log*

# ── Final pre-commit additions (additive; verified to match no real source) ──
# Extra secret / env filename variants
secrets.*
local.env

# Local tooling / editors
.cursor/

# QA / performance evidence (generated, never source)
qa-screenshots/
lighthouse/
lighthouse-results/
traces/
*.har

# Local exports / scratch / backups
exports/
local-exports/
debug/
*.bak
